#!/usr/bin/python

os:

-------------------------------------------------------------------------------
"List of os module's Data and Methods (Python 2.7.5)"

'Data'
linesep 	换行符，根据系统的不同而不同，posix下为 '\n'
seq			路径/url分隔符，系统不同值不同， poxit下为 '/'
SEEK_CUR =1	文件当前位置
SEEK_END =2	文件末尾位置
SEEK_SET =0	文件起始位置

'Method'
listdir(path)	查看path下的所有文件和目录(包括隐藏文件目录，但不包括'.'和'..')
getcwd()		查看当前所在路径
chdir(path)		切换当前目录到path
system(cmd)		运行系统命令
-------------------------------------------------------------------------------


os.path
--------------------------------------------------------------------------------
"List of os.path moudle's attributes (Python 2.7.5)"

'Method'
exists(path)	查看路径(文件/目录)是否存在

------------------------------------------------------------------------------


